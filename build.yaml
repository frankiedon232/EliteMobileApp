targets:
  $default:
    builders:
      json_serializable:
        options:
          # Models mirror docs/API_CONTRACT.md: snake_case keys.
          field_rename: snake
          explicit_to_json: true
          include_if_null: true
